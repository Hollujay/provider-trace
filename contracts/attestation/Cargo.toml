[package]
name = "provider-trace-attestation"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
soroban-sdk = "27.0.2"
thiserror = "2"

[dev-dependencies]
soroban-sdk = { version = "27.0.2", features = ["testutils"] }
